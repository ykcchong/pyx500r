"""LibraryView browser example for pyx500r.

A FastAPI backend (``app.py``) + bundled single-page GUI (``static/``) for
browsing a SCIEX LibraryView ``.sqlite`` database: compounds, their settings,
and their reference spectra (rendered as plots).
"""
